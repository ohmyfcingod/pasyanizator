# Зачистка плейсхолдеров в New-ВидПоЭталону завязана на LF-литералы ("{{...}}`n") —
# фиксируем фактический контракт шаблонов, чтобы свежий клон с autocrlf=true их не поломал
# (ревью П3; переход на CRLF — вместе с нормализацией EOL врезки в такте 4, ОБА конца разом).
*.tpl text eol=lf
