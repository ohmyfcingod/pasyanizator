#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

// ПАСЬЯНИЗАТОР (генерировано из Шаблоны/ObjectModule.bsl.tpl, руками не править):
// пересчёт дескрипторов доступа при записи — как у ДокументыПредприятия (ObjectModule:1028).

Процедура ПриЗаписи(Отказ)

	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;

	Если ДокументооборотПраваДоступаПовтИсп.ВключеноИспользованиеПравДоступа() Тогда
		ДокументооборотПраваДоступа.ОпределитьДескрипторыОбъекта(ЭтотОбъект);
	КонецЕсли;

КонецПроцедуры

// UX: новый элемент вида сразу несёт свой классификатор-ВидДокумента (как заводской документ
// знает свой вид). {{ВИД_UUID}} — uuid ВидаДокумента этого справочника (подставляет генератор).
Процедура ОбработкаЗаполнения(ДанныеЗаполнения, СтандартнаяОбработка)

	Если НЕ ЗначениеЗаполнено(ВидДокумента) Тогда
		Попытка
			ВидДокумента = Справочники.ВидыДокументов.ПолучитьСсылку(
				Новый УникальныйИдентификатор("{{ВИД_UUID}}"));
		Исключение
			// реквизита ВидДокумента может не быть в редких видах — заполнение не настаивает
		КонецПопытки;
	КонецЕсли;

КонецПроцедуры

#КонецЕсли
