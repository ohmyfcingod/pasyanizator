// ПАСЬЯНИЗАТОР (генерировано): форма элемента сгенерённого вида (такт 5, П2). ПОСТАВЛЯЕТСЯ вместе
// с видом (НЕ временный харнесс — маркер «-ВРЕМЕННО» тут был мислейблом, поймано Test-ЧистотаПоставки).
// Отбор по владельцу для списочных реквизитов (значения в общем Catalog.ЗначенияСвойствОбъектов):
// фиксированный Отбор.Владелец в .mdo НЕВОЗМОЖЕН (владелец = данные, не predefined) — ставим
// программно здесь, UUID свойства известен генератору. Форма-элемента с этим модулем =
// восстановленная целостность выбора при варианте А (FK на общий справочник).
// ЗАР (УДАР 3 деда): рантайм-отбор работает ТОЛЬКО для поля НА ЭТОЙ форме. В ДИНАМИЧЕСКОМ
// СПИСКЕ (журнал документов) отбор по этому реквизиту через стандартную лупу покажет ВЕСЬ
// справочник ЗначенияСвойствОбъектов (значения всех видов) — динсписок не знает про этот код.
// Приемлемый техдолг V1; строгий отбор в списках — только кастомные формы списков (такт 7 UI).

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)

{{УСТАНОВКА_ОТБОРОВ}}
КонецПроцедуры
